# Approval Flow Example

Example flow:

```text
_raw -> _drafts -> _meta/review -> approve/reject -> canonical folders
```

This is intentionally generic and should be adapted to your execution layer.

