# Auto Ingest Example

This note describes an optional enhancement pattern where new `_raw/` items are periodically turned into `_drafts/`, without skipping review.

This should remain conservative and never auto-publish by default.

